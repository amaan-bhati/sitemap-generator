name: Generate Sitemap

on:
  schedule:
    # Run every Sunday at 2 AM UTC
    - cron: '0 2 * * 0'
  workflow_dispatch:  # Allow manual trigger

jobs:
  generate-sitemap:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp beautifulsoup4 lxml
      
      - name: Create sitemaps directory
        run: mkdir -p sitemaps
      
      - name: Generate sitemap
        run: python sitemap_generator.py
      
      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add sitemaps/
          git commit -m "🤖 Auto-generated sitemap - $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
      
      - name: Upload sitemap as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sitemaps
          path: sitemaps/
          retention-days: 30
      
      - name: Create GitHub Release with sitemap
        if: always()
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: sitemap-${{ github.run_number }}
          release_name: Sitemap Generation Run #${{ github.run_number }}
          body: |
            Automated sitemap generation run
            Generated at: ${{ github.event.head_commit.timestamp }}
          draft: false
          prerelease: false